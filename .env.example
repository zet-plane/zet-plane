# Canonical config source: apps/server/config.yaml (loaded by AppConfig).
# .env is no longer required for the NestJS server. Variables below remain
# only as Prisma CLI (`prisma migrate`, `prisma generate`) overrides — the CLI
# runs outside Nest DI and reads process.env directly.
DATABASE_URL=postgresql://user:password@localhost:5432/zet_plane
